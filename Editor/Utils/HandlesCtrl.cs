using UnityEngine;

namespace LZ.WarGameMap.MapEditor
{
    public class HandlesCtrl : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
